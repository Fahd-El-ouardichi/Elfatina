Drop your .glb design files here (e.g. sahara.glb).
Then in assets/js/showroom.js set:  file: "../assets/models/sahara.glb"
Name the materials inside the model: fabric / hijab / trim 
so the studio color pickers can recolor the right parts.
